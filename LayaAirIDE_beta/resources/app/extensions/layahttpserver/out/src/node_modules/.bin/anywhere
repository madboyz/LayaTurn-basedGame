#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../anywhere/bin/anywhere" "$@"
  ret=$?
else 
  node  "$basedir/../anywhere/bin/anywhere" "$@"
  ret=$?
fi
exit $ret
