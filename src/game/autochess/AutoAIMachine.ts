import { BaseMachine } from "../machine/BaseMachine";

export class AutoAIMachine extends BaseMachine {
    
    constructor()
    {
        super();
    }
    public Init(info:Array<any>)
    {
        super.init(info);
    }

    public update():void{
        super.update();
    }
}